[tool.poetry]
name = "ml_project_demo"
version = "0.1.0"
description = "Demo ml project"
authors = ["Golgovskiy"]
readme = "README.md"
repository = "https://github.com/Golgovskiy/ml-project-demo"
keywords = ["demo"]

[tool.poetry.dependencies]
python = "^3.8"
click = "^8.1.3"
pandas = "^1.4.2"

[tool.poetry.dev-dependencies]
pytest = "^7.1.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
ml_project_demo = "ml_project_demo.console:main"